<body data-turbo="false">
    <div class="container">
    <h1 class="display-4">Bienvenue ! <%= params[:id] %></h1>
    <p>Vous voici arrivé sur <b>the</b> site des potins. Un p'tit apperçu des doss' déjà connus :</p>
    <br /> <br />

    <div class="row">
        <% @gossips.each do |gossip| %>
    <div class="col-md-3">
      <div class="row">
        <div class="col-md-12">
          <div class="card mb-3 card-home">
            <div class="card-body mt-auto mb-auto">
              <h5 class="card-title"><%= gossip.title %></h5>
              <p class="card-text"><em><%= gossip.content %></em></p>
              <a href="potin/<%=gossip.id%>" class="btn btn-danger">Voir le potin</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
    </div>
 <%= yield %>

</body>